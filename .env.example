# Copy to .env and fill in. Never commit .env.

# AWS
AWS_PROFILE=default
AWS_REGION=us-east-1

# Terraform (optional overrides; terraform.tfvars covers defaults per environment)
# TF_VAR_project_name=agent-security-lab
# TF_VAR_environment=dev
