# Terraform modules

Empty for now. Modules are added per phase as components are implemented:

- `agentcore-gateway/` — Phase 1 (gateway)
- `agentcore-runtime/` — Phase 2–3 (delegated agents, orchestrator)
- `interceptor-lambda/` — Phase 4 (interceptor + OPA)
- `approval-workflow/` — Phase 5 (DynamoDB + KMS)
