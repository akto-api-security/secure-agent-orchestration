# Terraform modules

Empty for now. Modules are added per phase as components are implemented:

- `agentcore-runtime/` — Phase 1 (agents)
- `agentcore-gateway/` — Phase 2 (gateway + interceptor)
- `interceptor-lambda/` — Phase 2 (gateway + interceptor)
- `approval-workflow/` — Phase 4 (DynamoDB + KMS)
