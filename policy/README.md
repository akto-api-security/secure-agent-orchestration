# Policy

Placeholder. Implemented in Phase 3: OPA policies enforced inside the
REQUEST interceptor Lambda (see `gateway/`), covering deterministic approval
and human-in-the-loop rules.

No policies yet.
