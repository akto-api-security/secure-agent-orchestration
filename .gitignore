# Environment / secrets
.env
.env.*
!.env.example

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars.local
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl.bak

# Phase 6: account-specific partial backend config (generated by
# scripts/bootstrap.sh from infra/bootstrap's own output) -- see
# backend.hcl.example for the template.
backend.hcl

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Internal-only docs (not for client sharing)
/docs/

# Internal-only test scripts (hardcode this account's specific ARNs/runtime
# IDs, invoke real AWS resources -- not for client sharing)
/scripts/test_sendfeedback_block.sh
/scripts/test_approval_flow.sh
/scripts/test_hitl_flow.sh
/scripts/approval_decide.py

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code harness working files (not project content)
.claude/
