__pycache__/
*.pyc
.pytest_cache/

# Local credentials for manual dry-run testing against a real deployment.
# Never commit real tokens.
.env.local

# Internal engineering notes from building this SDK against the real
# Guardrail Engine's source — useful for future SDK development, not for
# customer distribution. Kept on disk locally, never committed/pushed.
API_CONTRACT_CHECKLIST.md
akto-guardrail-api-contract-answers.md
